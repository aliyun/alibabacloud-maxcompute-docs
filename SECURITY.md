# 安全说明

## 报告安全问题

发现可能泄露凭据、客户数据或影响文档站点供应链的问题时，不要在公开 Issue、合并请求或文档页面中披露漏洞细节、令牌和客户数据。请使用组织内已经批准的安全事件报告渠道，并只向有处置权限的安全人员和项目 Owner 提供详情。

当前仓库未登记且未验证项目专用的私密报告入口。正式开放贡献前，项目管理员必须完成以下事项：

1. 在 GitLab 项目中启用可用的私密漏洞报告能力，或确定其他经批准的内部渠道。
2. 以真实链接或联系方式替换本段，并验证普通贡献者具备报告权限。
3. 明确接收人、首次响应时限、升级方式和误提交敏感信息后的清理流程。

## 内容安全边界

- 不提交 AccessKey、令牌、Cookie、客户数据、真实账号或个人信息。
- 示例使用规范占位符和脱敏数据。
- 外部脚本、iframe、分析 SDK、构建插件和新 npm 依赖需要安全审查。
- CI 和部署凭据只存放在受保护、已屏蔽的 GitLab CI 变量中。
- 不使用 `npm audit --force` 自动改写依赖树；依赖升级应通过可审查的合并请求完成。
