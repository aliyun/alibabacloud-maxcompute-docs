# ADR 0003：生产托管方案待确定

- 状态：待决策
- 日期：2026-08-28

## 背景

仓库初始化时没有提供生产域名、可见性、发布平台和凭据管理要求。

## 临时决策

CI 只生成并保存静态站点制品，不添加看似可用但缺少真实目标的发布任务。站点地址和子路径通过环境变量覆盖本地默认值。

## 完成条件

确认托管平台、真实 URL、访问范围、缓存、安全响应头、发布权限、回滚和搜索方案后，新增正式 ADR 并替代本条临时决策。
