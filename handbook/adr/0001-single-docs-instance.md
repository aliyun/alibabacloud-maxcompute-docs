# ADR 0001：初始使用单一 docs 实例

- 状态：已接受
- 日期：2026-08-28

## 背景

平台需要托管多个组件，但初始化阶段尚无组件独立维护多个公开版本的明确需求。

## 决策

使用一个默认 docs plugin、稳定组件目录和多套 sidebar。组件注册表驱动首页入口，校验脚本保证目录和登记一致。

## 结果

该方案减少插件、版本目录和构建配置。某组件出现独立版本生命周期后，可以保持原 URL 迁移为独立实例。组件规模大到共享构建不合理时，需要重新评估独立站点。
