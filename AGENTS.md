# AGENTS.md

本文件定义自动化代理和工程协作者在本仓库中的工作边界。

## 仓库目标

本仓库是 MaxCompute 的统一文档站点。站点使用一套导航、搜索、贡献契约和质量门禁承载多个产品文档空间，让各产品团队可以独立维护内容，同时为读者提供一致体验。

一期公开产品范围固定为：

- `cli`：MaxCompute CLI
- `mcp-server`：MaxCompute MCP Server
- `desktop`：MaxCompute Desktop

不在一期范围内的候选产品只记录在 `handbook/roadmap.md`，不得提前加入公开导航、首页入口或产品注册表。

## 权威文件

- `config/products.json`：产品 ID、名称、摘要、状态、文档入口和所有权的单一数据源。
- `config/products.schema.json`：产品登记契约。
- `docusaurus.config.ts`：站点、插件、语言和导航配置。
- `sidebars.ts`：各内容域的 sidebar 入口。
- `CONTRIBUTING.md`：贡献流程和内容规则。
- `handbook/`：架构、产品接入、路线图、版本和部署决策。
- `templates/`：新文档的起始模板。

## 修改规则

1. 修改内容前先识别产品 ID、文档类型、目标读者和事实来源。
2. 新产品必须同步更新产品注册表、目录、首页入口、导航和 CODEOWNERS。
3. 不编造产品行为、命令参数、控制台路径、地域支持、权限、错误码、性能结果或客户案例。来源不足时保留骨架并标注内容状态。
4. 不在仓库中写入凭据、客户数据、内部访问令牌、真实账号或未脱敏截图。
5. MDX 中的 JSX、原始 HTML、脚本、iframe 和第三方资源按代码变更处理。
6. 不直接修改 `build/`、`.docusaurus/`、`node_modules/` 或版本化生成目录。
7. 不引入第二种包管理器或第二个锁文件。
8. 新依赖必须说明用途、维护状态和不能使用现有能力完成的原因。
9. 不随意修改已发布路径。移动或删除页面时同时处理重定向和引用。
10. 只有任务明确指向历史版本时才修改历史版本文档。

产品目录中的更具体 `AGENTS.md` 可以收紧这些规则，但不得放宽安全和事实准确性要求。

## 站点边界

- 默认保持“一个站点 + 多产品文档空间”，不得仅因产品名称、团队或侧栏不同而复制 Docusaurus 站点。
- 每个产品可以拥有独立 sidebar、发布节奏和内容所有者，但共享站点外壳、搜索、构建和发布流程。
- 只有出现访问控制、独立部署、构建规模或版本生命周期无法共享的明确约束时，才通过 ADR 评估独立站点或独立 docs plugin。

## 验证命令

```shell
npm ci
npm run ci
```

所有提交都应运行完整 `npm run ci`。如果环境限制导致生产构建无法执行，在完成报告和合并请求中明确记录未执行项、原因和替代验证，不得将 `npm run validate` 描述为完整验收。

## 完成报告

交付说明应分别列出：

- 已修改的内容
- 已执行并通过的验证
- 因环境或权限未执行的验证
- 仍需真实维护者确认的产品事实、所有权或部署参数
