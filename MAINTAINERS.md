# 维护责任

## 平台维护

仓库命名空间 `@odps` 是初始化阶段的平台兜底所有者。实际审批由 GitLab 项目成员、受保护分支和 `.gitlab/CODEOWNERS` 共同约束。

平台维护范围包括：

- Docusaurus 配置、主题和依赖
- 产品注册表和校验脚本
- GitLab CI、模板和发布流程
- 公共导航、URL 和内容规范

## 产品维护

每个产品接入时，应在 `.gitlab/CODEOWNERS` 中登记比 `@odps` 更具体的真实维护组。产品维护者负责产品事实、示例、兼容性、版本、弃用和归档。

注册表中的 `owner` 与 CODEOWNERS 应保持一致。维护者变化时，在同一个合并请求中更新两处并完成产品团队确认。
